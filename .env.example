# Copy to .env and fill this in locally. Never commit .env.
OPENROUTER_API_KEY=
# For DICA, use apiKeyEnv: "DICA_API_KEY" in your configuration.
# DICA_API_KEY=
# Optional: use ONE shared file for all plugin instances instead of this file.
# KILO_HANDOFF_ENV_FILE=C:/Users/YOUR_NAME/.config/kilo/handoff.env
# Optional: one central JSON configuration for all plugin instances.
# KILO_HANDOFF_CONFIG=D:/data/code/kilo-handoff/config.local.json
